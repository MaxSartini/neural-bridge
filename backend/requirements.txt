# ===========================================
# Neural Bridge Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Web Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= HTTP Utilities =============
# requests is imported by the vendored TRIBE runtime package.
requests>=2.28.0

# ============= Data Processing =============
# Required by benchmark scripts and calibration utilities.
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
# Grouped holdout evaluation and benchmark helpers
scikit-learn>=1.5.0

# ============= Neuro / ML Runtime =============
# Core Torch/Transformers runtime for TRIBE adapters, V-JEPA2, and optional
# encoder paths. On Apple Silicon, install a PyTorch build with MPS support.
torch>=2.3.0
transformers>=4.44.0
safetensors>=0.4.0
huggingface-hub>=0.24.0
tqdm>=4.66.0
nibabel>=5.2.0
nilearn>=0.10.0

# Apple Silicon / MLX acceleration paths.
mlx>=0.20.0
mlx-lm>=0.19.0

# TRIBE extractor support imported by the active Apple Silicon/MLX path.
# These are pinned because the released TRIBE config is sensitive to
# neuralset/exca schema drift.
neuralset==0.0.3
neuraltrain==0.0.3
exca==0.5.20
einops>=0.8.0
x-transformers>=2.21.0
lightning>=2.3.0
mne>=1.8.0
torchmetrics>=1.4.0
PyYAML>=6.0.0
moviepy>=2.2.0
soundfile>=0.14.0
mlx-whisper>=0.4.0

# ============= Utilities =============
# .env loading
python-dotenv>=1.0.0
# Data validation used by TRIBE/neuralset adapters
pydantic>=2.0.0
