# ===========================================
# Neural Bridge Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Web Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM / Inference Clients =============
# OpenAI SDK — unified OpenAI-compatible client (LM Studio + Ollama)
openai>=1.0.0
# httpx — service health pre-flight (run.py) and async HTTP fallback
httpx>=0.27.0
# requests — HTTP client used by Ollama embedding paths and ad-hoc calls
requests>=2.28.0

# ============= Graph Database =============
neo4j>=5.15.0

# ============= Multi-Agent Simulation (OASIS / CAMEL-AI) =============
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= Data Processing =============
# Required by benchmark scripts, calibration utilities, and file_parser
# CSV/XLSX ingestion.
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
# Supervised neuro-to-behaviour calibration and grouped holdout evaluation
catboost>=1.2.8
scikit-learn>=1.5.0
# openpyxl — pandas backend for .xlsx uploads
openpyxl>=3.1.0

# ============= Neuro / ML Runtime =============
# Core Torch/Transformers runtime for TRIBE adapters, V-JEPA2, and optional
# encoder paths. On Apple Silicon, install a PyTorch build with MPS support.
torch>=2.3.0
transformers>=4.44.0
safetensors>=0.4.0
huggingface-hub>=0.24.0
tqdm>=4.66.0
nibabel>=5.2.0
nilearn>=0.10.0

# Apple Silicon / MLX acceleration paths.
mlx>=0.20.0
mlx-lm>=0.19.0

# Research runtime packages imported by optional encoder adapters.
momentfm>=0.1.0
neuralset>=0.1.0
exca>=0.5.0

# ============= File Parsing =============
# PDF text extraction
PyMuPDF>=1.24.0
# Image handling for screenshot_processor (vision pipeline)
Pillow>=10.0.0
# Encoding detection for non-UTF-8 text files
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utilities =============
# .env loading
python-dotenv>=1.0.0
# Data validation (used in models, ontology generation)
pydantic>=2.0.0
