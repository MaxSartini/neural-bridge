# VEATIC 2.1 Phase 05 — Learned Frozen-AR Bridge

This compact concluded record documents the no-washout learned residual-head discovery on the
sealed width-64 current-row Phase 04 representation. The implementation lives in
`src/neural_bridge/veatic21/residual.py` and `src/neural_bridge/veatic21/phase05.py`; the
authorized entrypoint is:

```text
uv run python -m neural_bridge.veatic21 phase05
```

The full 144-checkpoint search, exact selected checkpoint and prediction bundles, fold and
per-video metrics, and checksum inventory remain under the external root recorded in
`provenance.json`. This directory retains the defensible request, selected recipe, control
matrix, compact result and summary, primary deltas, derivation ledger, report, external
artifact manifest, and provenance.

Phase 05 passed its execution and integrity gate but the no-washout residual scientific claim
failed. The width-8 ReLU bottleneck was selected using inner evidence; only three of six real
residual checkpoints earned positive inner value, and the blocked real lane fell back
bit-exactly to frozen AR. Phase 06 is not authorized. Legal persistence dominance activates
only the preregistered VEATIC washout-design procedure; no washout target or cortical washout
candidate was constructed or scored here.
