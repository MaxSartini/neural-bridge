# VEATIC 2.1 Phase 04 — Fold-Owned PCA Bridge

This compact concluded record documents the fold-owned maximum rank-512 PCA bridge and
control-complete inner-only representation search for the final TRIBE
`cortical_prediction`. The implementation lives in `src/neural_bridge/veatic21/pca.py` and
`src/neural_bridge/veatic21/phase04.py`; the authorized entrypoint is:

```text
uv run python -m neural_bridge.veatic21 phase04
```

The full score caches, exact 17-lane prediction bundles, fitted PCA and final model bundles,
fold/per-video metrics, and checksum inventory remain under the external root recorded in
`provenance.json`. This directory retains the defensible request, accuracy audit, selected
representation, compact result and summary, primary deltas, derivation ledger, report,
external artifact manifest, and provenance.

Phase 04 passed its execution and integrity gate. The inner-only selector froze width 64 with
current-row context, but the linear PCA-fusion scientific claim failed because it did not
consistently beat frozen AR and matched controls. Linear PCA fusion is not promoted. The
record authorizes only Phase 05 VEATIC learned frozen-AR residual bridge discovery on this
sealed representation; it does not authorize washout activation or later phases.
