# VEATIC 2.1 Phase 02 — Fresh Target-Specific AR Baseline

This compact concluded record documents the sealed Phase 02 autoregressive floor for the
VEATIC-selected `t+1..t+6` future-maximum-increase target. The implementation lives in
`src/neural_bridge/veatic21/ar.py` and `src/neural_bridge/veatic21/phase02.py`; the authorized
entrypoint is:

```text
uv run python -m neural_bridge.veatic21 phase02
```

The full audit payload, exact split ownership, fitted model bundles, and frozen prediction
bundles remain under the external root recorded in `provenance.json`. This directory retains
the defensible request/configuration, compact result, selected hyperparameters, AR-dominance
decomposition, derivation ledger, report, external artifact manifest, and provenance.

Phase 02 passed all mandatory controls. It authorizes only Phase 03 raw cortical benchmarking
on the exact matched rows and frozen AR predictions. It does not authorize PCA, washout
activation, learned bridge work, or later phases.
