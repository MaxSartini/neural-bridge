# VEATIC 2.1 Phase 03 — Raw Predicted-Cortical Benchmark

This compact concluded record documents the control-complete full-width raw benchmark for
the final TRIBE `cortical_prediction`. The implementation lives in
`src/neural_bridge/veatic21/raw_cortical.py` and `src/neural_bridge/veatic21/phase03.py`; the
authorized entrypoint is:

```text
uv run python -m neural_bridge.veatic21 phase03
```

The full audit payload, exact 17-lane prediction bundles, fitted full-width model bundles,
per-video metrics, and input identity records remain under the external root recorded in
`provenance.json`. This directory retains the defensible request/configuration, control
matrix, compact result and summary, primary deltas, derivation ledger, report, external
artifact manifest, and provenance.

Phase 03 passed its execution and integrity gate but the direct raw-fusion scientific claim
failed. Direct fusion is not promoted. The record authorizes only Phase 04 fold-owned PCA and
temporal representation search on the exact matched Phase 02 ownership; it does not authorize
washout activation, learned bridge work, or Phase 05.
