# Neural Bridge Repository Contract

- This is the canonical private Neural Bridge repository. Treat
  `/Users/maxsartini/Neural Bridge` and
  `/Volumes/onn. Drive/Neural Bridge Artifacts` as one logical codebase.
- Work only on `main`. Do not create branches. Commit and push coherent transitions directly
  to `origin/main`.
- Before any Neural Bridge action, read this file and then read
  `/Users/maxsartini/Neural Bridge/internal/handoff/CURRENT_STATE.md` completely.
- `CURRENT_STATE.md` is the only live programme state and the only source of current
  authorization. No other plan, specification, protocol, registration, study summary, or
  run report may authorize VEATIC work.
- Follow the ordered work in `CURRENT_STATE.md`: establish the evidence foundation first,
  then autonomously design, implement, train, infer, benchmark, and iterate the fresh VEATIC
  2.1 programme. Computation is authorized whenever it is useful. Do not pause for another
  authorization between those steps.
- Never delete, rewrite, move, normalize in place, or regenerate these roots:
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/tribe-v2/`
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/vjepa-2.1/`
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/veatic-2.1/`
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/runs/again/`
- The `tribe-v2` and `vjepa-2.1` roots are retired from downstream use. Preserve them, but do
  not invoke, enumerate, read, hash, copy from, or use them for audits, modelling, or
  reconstruction. Do not rerun V-JEPA or TRIBE.
- The mandatory and exclusive source of VEATIC 2.1 cortical predictions is
  `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/veatic-2.1/`. This does not
  prohibit reading required VEATIC labels, annotations, repository material, or audit
  evidence outside the two retired roots.
- Preserve unrelated changes. Heavy generated artifacts belong only under
  `/Volumes/onn. Drive/Neural Bridge Artifacts`.
