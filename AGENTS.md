# Neural Bridge Repository Contract

- This is the canonical private Neural Bridge repository.
- Treat `/Users/maxsartini/Neural Bridge` and `/Volumes/onn. Drive/Neural Bridge Artifacts` as one logical codebase.
- Before any VEATIC scientific action, read completely and in order: this file; `/Users/maxsartini/Neural Bridge/internal/active/veatic21-master-scientific-specification.md`; and `/Users/maxsartini/Neural Bridge/internal/handoff/CURRENT_STATE.md`. Then read only the sections of `/Users/maxsartini/Neural Bridge/internal/active/veatic21-rebuild-protocol.md` explicitly named by the current state for the authorized action.
- `/Users/maxsartini/Neural Bridge/internal/active/veatic21-master-scientific-specification.md` is the permanent comprehensive authority for durable VEATIC input boundaries, scientific method, controls, metrics, provenance, phase order, and implementation contracts. Never delete, replace wholesale, or shorten it during routine handoff or phase progression. Amend it in place only for an explicitly authorized durable method change.
- `/Users/maxsartini/Neural Bridge/internal/active/veatic21-rebuild-protocol.md` is a derived operational checklist and navigation aid, not an independent source of scientific truth. It may summarize but never add, weaken, replace, or override the master specification. If it differs from the master, the master wins and the checklist must be corrected before execution.
- `/Users/maxsartini/Neural Bridge/internal/handoff/CURRENT_STATE.md` is the replace-in-place authority for live progress, exact result/artifact hashes, current authorization, blockers, and the exact next action. Every replacement must retain its `Mandatory authority anchors` section and exact master/protocol paths. Do not duplicate the master specification in the live handoff.
- `CURRENT_STATE.md` cannot silently override the master specification. When a durable method change is required, update the master specification, rebuild protocol, and current state together; run the authority-contract tests; commit and push before executing the changed method.
- Archive, legacy, sealed superseded, previous-rebuild, and old-study material is never current authority unless the user explicitly requests historical work.
- Do not infer current scientific semantics or numeric choices from historical files.
- Execute a scientific next action only when the active task authorizes it.
- Store heavy artifacts only under `/Volumes/onn. Drive/Neural Bridge Artifacts`. Canonical paths encode programme, phase, role, and lifecycle.
- Keep concluded results front-facing; keep active work, decisions, and current state under `internal/`. Each concluded phase owns only its defensible runner, config, compact results, report, provenance, artifact manifest, and tests.
- Build VEATIC 2.1 only from VEATIC-specific targets, projections, models, controls, and selection. AGAIN contributes compatible hypotheses and methodological rigor patterns only, never code, runners, fitted artifacts, or numeric choices.
- Keep VEATIC 2.1 and AGAIN implementations separate. The active VEATIC rebuild must not import, execute, copy, adapt in place, or reuse AGAIN code, phase runners, data, fitted artifacts, or numeric selections.
- Preserve unrelated changes.
