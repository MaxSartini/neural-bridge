# Neural Bridge Repository Contract

- This is the canonical private Neural Bridge repository. Treat
  `/Users/maxsartini/Neural Bridge` and
  `/Volumes/onn. Drive/Neural Bridge Artifacts` as one logical codebase.
- Work only on `main`. Do not create branches. Commit and push each coherent scientific or
  authority transition directly to `origin/main` before executing the next transition.
- Before any VEATIC scientific action, read completely and in this order:
  1. this file;
  2. `/Users/maxsartini/Neural Bridge/internal/active/veatic21-master-scientific-specification.md`;
  3. `/Users/maxsartini/Neural Bridge/internal/active/veatic21-supervised-spike-continuous-combination.md`;
  4. `/Users/maxsartini/Neural Bridge/internal/handoff/CURRENT_STATE.md`;
  5. the sections of
     `/Users/maxsartini/Neural Bridge/internal/active/veatic21-rebuild-protocol.md` named by
     `CURRENT_STATE.md`.
- Authority order is: this contract; the master scientific specification; the supervised
  combination specification; the operational protocol; the live current state. The current
  state may authorize work but may not change scientific method.
- The active VEATIC route is exactly: sealed 124-video input; VEATIC-owned target, geometry,
  and ownership derivation; one consolidated supervised arousal build containing the best
  independently validated spike and continuous specialists; valence; genuine zero-label
  inference; paper/product transition.
- The master scientific specification is the durable VEATIC method. Change it only when the
  user authorizes a durable method change. Update every dependent active authority and test
  in the same commit.
- The supervised combination specification owns the exact dependency structure for the
  strongest supervised spike + continuous build. Every fitted value and numeric choice must
  be derived freshly from VEATIC ownership.
- The rebuild protocol is an operational checklist derived from the master and combination
  specifications. It may not add, weaken, or replace their method.
- `CURRENT_STATE.md` is replaced in place at each transition. It owns exact live status,
  hashes, authorization, blockers, and one next action. It must retain its mandatory
  authority anchors.
- The only downstream VEATIC input is
  `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/neural-bridge-input/`.
  Every scientific build must use all explicit `per_video/0` through `per_video/123`
  directories and join rows only within the matching video directory.
- Never delete, rewrite, move, or regenerate any of these roots:
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/tribe-v2/`
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/vjepa-2.1/`
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/features/veatic-2.1/neural-bridge-input/`
  - `/Volumes/onn. Drive/Neural Bridge Artifacts/runs/again/`
- V-JEPA 2.1 and TRIBE v2 are one completed upstream stack. Do not rerun either. Do not open,
  map, hash, copy, or inspect `vjepa21_hidden_states.npz`.
- AGAIN supplies only the traced structural hypothesis and rigor pattern. VEATIC must not
  import, execute, copy, adapt in place, or reuse AGAIN code, runners, data, fitted artifacts,
  predictions, or numeric selections.
- Heavy artifacts live only under
  `/Volumes/onn. Drive/Neural Bridge Artifacts/runs/veatic-2.1/`. Preserve unrelated changes.
