# Phase 6 Seed-27 Convergence Diagnostic

Historical development evidence for the post-hoc convergence audit of seed `20260627`. This diagnostic helped rule out tuned under-training; it is not a promoted result and does not replace the later successful original three-checkpoint ensemble confirmations.

The tracked snapshot contains lightweight CSV/JSON diagnostics only. Checkpoints, arrays, frozen score caches, and other heavy runtime artifacts remain outside git.
