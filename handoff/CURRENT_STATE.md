# Current State

Updated: 2026-07-20

## Current result

- This repository is the canonical private Neural Bridge `main`; the pre-rebuild Git history and a pruned, hash-verified copy of its former working tree remain preserved externally.
- `src/neural_bridge/again/` is the supported AGAIN implementation for CPU, CUDA, and MLX. Phase 5 and both Phase 7 checkpoint replays match published rows below `6e-8`.
- Phase 7's claim-bearing grouped held-out-video result covered `420/420` declared cells and `15/15` positive fold-checkpoint groups. Its separately scoped blocked-temporal protocol did not pass and remains separate evidence.
- `src/neural_bridge/zero_label/` recomputes the locked 140-row verdict, validates no-label inference audits and the prediction seal, and hash-verifies the registered 95-file run.
- Ruff, ty, and all 13 supported tests pass. Archived reproduction snapshots are evidence, not a linted current API.

## Storage and agent workflow

- `/Users/maxsartini/Neural Bridge` is the only live code root. Its ignored `artifacts` link resolves to `/Volumes/onn. Drive/Neural Bridge Artifacts`.
- The canonical artifact registry is indexed in Context-Mode as `neural-bridge-canonical-artifacts`; CodeGraph indexes the canonical code root and excludes heavy artifacts.
- The former external root was atomically moved and arrival-verified, then carved into canonical foundations. Its retained archive is now `25,288,674,225` bytes with tree hash `69ac4f8c…cf611`; the arrival hash remains recorded.
- The former internal working tree was copy-verified externally and its redundant internal source was deleted with approval. Reproducible environments/tools were removed; the retained archive is `37,057,628,689` bytes with tree hash `8ce1ef45…ed2f`.
- Canonical model storage now protects complete and exact-payload PyTorch/CUDA and MLX forms of V-JEPA 2.1 plus PyTorch/CUDA and MLX TRIBE v2. See `registry/artifacts/encoder-model-foundation-20260720.json`.
- Protected feature foundations include the joint `41,473,940,652`-byte AGAIN V-JEPA 2.1 + TRIBE v2 dense cache and both VEATIC 2.1 compact caches. Original AGAIN and VEATIC videos, labels/ratings, and provenance remain canonical.
- Proven duplicates and reproducible cache/environment debris were deleted through `internal/migration/deletion-manifest.csv`, freeing about 39 GB internally and 50 GB externally. BEAT, audio/transcription models, smaller encoder variants, results, and benchmarks remain retained.
- Current free space is approximately 139 GiB on the internal data volume and 249 GiB on the external SSD.
- Restart verification passed: all 11 hooks are enabled/trusted; Context-Mode, CodeGraph, RTK, Ponytail, Caveman, Karpathy, project skills, and intended plugins are wired.

## Constraints

- Do not launch canonical VEATIC 2.1 training or tuning during foundation audit and migration work.
- Build VEATIC 2.1 from its own 2 Hz data, targets, representations, fitted PCA, heads, and controls. AGAIN supplies hypotheses and rigor utilities only after semantic compatibility is proven.
- Prioritize an exceptional VEATIC event/spike system first; use that representation to pursue continuous arousal and valence when evidence supports the sequence. Zero-label optimization follows the VEATIC result.
- Never promote discovery, smoke, inner-only, or contract-invalid evidence into a scientific claim. Preserve split, fold, seed, target, control, locked-pool, and provenance boundaries.

## VEATIC 2.1 fresh-start boundary

- `src/neural_bridge/veatic21/` is intentionally not implemented yet. The retired `backend/scripts/veatic21_*` system is forensic evidence only: it became AGAIN-shaped and must not be copied or resumed.
- Start from the canonical VEATIC foundation: exactly 124 videos and 20,657 dense 2 Hz rows, with the registered VEATIC-only V-JEPA 2.1 and TRIBE v2 compact caches. A new TRIBE cache is not currently required.
- Apply the locked black/high-duplicate causal-window mask consistently; 923 unusable rows cannot support a video claim and must not enter training or scoring. Keep eligible negative rows from zero-event videos in pooled PR-AUC, but never fabricate a per-video zero score.
- Refit every VEATIC PCA, normalizer, target, threshold, AR model, representation, head, weight, and control inside its declared training ownership. Never import an AGAIN fitted artifact, head, threshold, locked-pool row, or model-selection decision.
- AGAIN Phase 5/7 and original VEATIC may contribute hypotheses, failure modes, architecture ideas, and neutral rigor utilities only after semantic compatibility is demonstrated. Fresh VEATIC discovery decides which representations and heads survive.
- Treat AGAIN Phases 0--5/5.5 as a required design ledger, not just Phase 5/7 as model inspiration: Phase 0 supplies dense-row, timestamp, mask, and failure-ledger discipline; Phase 1 supplies label alignment, eligibility, history, and train-owned-threshold rules; Phase 2 requires a target/protocol-specific frozen AR floor; Phase 3 proves raw predicted cortical features and direct AR fusion can damage performance; Phase 4 motivates train-fold-fitted PCA plus temporal aggregation while warning that fixed PCA can plateau; Phase 5/5.5 motivates learned residual/event-specific temporal heads, target rejection, and matched-control confirmation.
- Re-test those Phase 0--5 hypotheses on VEATIC rather than assuming their AGAIN winners transfer. Include clean raw, PCA-width, temporal/delta, residual, and head families where semantically valid; preserve negative results so discovery can distinguish a bad representation from a bad target, head, training regime, or control contract.
- Optimize the VEATIC event/spike system first. Use what it learns to pursue continuous arousal and valence when the evidence supports that sequence; do not force all three objectives into one recipe.
- Give candidate recipes a fair chance with validation-based overfit protection and convergence criteria; do not restore a fixed 80-epoch ceiling or treat a one-epoch probe as scientific evidence.
- Keep outer-test videos closed during discovery and selection. Smoke, dry-run, inner-only, and contract-invalid outputs are always non-promotable regardless of their numerical scores.

## Exact next action

Reload this handoff in a fresh VEATIC task. Do not start canonical training and do not port the retired executor. First build a new VEATIC-specific package and connect three numerical capabilities to the canonical VEATIC substrate: (1) nested discovery, (2) frozen-winner held-out confirmation cells, and (3) all-124 final refit/export. Smoke-test one target/fold/seed end to end through prediction sealing, resume, and audit; mark that smoke non-promotable. Only after the smoke proves the fresh data, mask, ownership, control, and no-AGAIN boundary may event/spike discovery begin.

Canonical detail: [`internal/migration/programme-map.md`](../internal/migration/programme-map.md) and [`studies/again/zero-label/README.md`](../studies/again/zero-label/README.md).
