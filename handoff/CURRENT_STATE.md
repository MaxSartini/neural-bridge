# Current State

Updated: 2026-07-20

## Current result

- This repository is the canonical private Neural Bridge `main`; the pre-rebuild Git history and a pruned, hash-verified copy of its former working tree remain preserved externally.
- `src/neural_bridge/again/` is the supported AGAIN implementation for CPU, CUDA, and MLX. Phase 5 and both Phase 7 checkpoint replays match published rows below `6e-8`.
- Phase 7's claim-bearing grouped held-out-video result covered `420/420` declared cells and `15/15` positive fold-checkpoint groups. Its separately scoped blocked-temporal protocol did not pass and remains separate evidence.
- `src/neural_bridge/zero_label/` recomputes the locked 140-row verdict, validates no-label inference audits and the prediction seal, and hash-verifies the registered 95-file run.
- Ruff, ty, and all 11 supported tests pass. Archived reproduction snapshots are evidence, not a linted current API.

## Storage and agent workflow

- `/Users/maxsartini/Neural Bridge` is the only live code root. Its ignored `artifacts` link resolves to `/Volumes/onn. Drive/Neural Bridge Artifacts`.
- The canonical artifact registry is indexed in Context-Mode as `neural-bridge-canonical-artifacts`; CodeGraph indexes the canonical code root and excludes heavy artifacts.
- The former external root was atomically moved and arrival-verified, then carved into canonical foundations. Its retained archive is now `25,288,674,225` bytes with tree hash `69ac4f8c…cf611`; the arrival hash remains recorded.
- The former internal working tree was copy-verified externally and its redundant internal source was deleted with approval. Reproducible environments/tools were removed; the retained archive is `37,057,628,689` bytes with tree hash `8ce1ef45…ed2f`.
- Canonical model storage now protects complete and exact-payload PyTorch/CUDA and MLX forms of V-JEPA 2.1 plus PyTorch/CUDA and MLX TRIBE v2. See `registry/artifacts/encoder-model-foundation-20260720.json`.
- Protected feature foundations include the joint `41,473,940,652`-byte AGAIN V-JEPA 2.1 + TRIBE v2 dense cache and both VEATIC 2.1 compact caches. Original AGAIN and VEATIC videos, labels/ratings, and provenance remain canonical.
- Proven duplicates and reproducible cache/environment debris were deleted through `internal/migration/deletion-manifest.csv`, freeing about 39 GB internally and 50 GB externally. BEAT, audio/transcription models, smaller encoder variants, results, and benchmarks remain retained.
- Current free space is approximately 139 GiB on the internal data volume and 249 GiB on the external SSD.
- Restart verification passed: all 11 hooks are enabled/trusted; Context-Mode, CodeGraph, RTK, Ponytail, Caveman, Karpathy, project skills, and intended plugins are wired.

## Constraints

- Do not launch canonical VEATIC 2.1 training or tuning during foundation audit and migration work.
- Build VEATIC 2.1 from its own 2 Hz data, targets, representations, fitted PCA, heads, and controls. AGAIN supplies hypotheses and rigor utilities only after semantic compatibility is proven.
- Prioritize an exceptional VEATIC event/spike system first; use that representation to pursue continuous arousal and valence when evidence supports the sequence. Zero-label optimization follows the VEATIC result.
- Never promote discovery, smoke, inner-only, or contract-invalid evidence into a scientific claim. Preserve split, fold, seed, target, control, locked-pool, and provenance boundaries.

## Exact next action

Reload this handoff in the waiting VEATIC task. Do not start canonical training. Finish and smoke-test the three pending numerical executor integrations first; then audit the VEATIC 2.1 foundation and prove that no AGAIN fitted artifact or locked pool crosses the boundary before defining fresh event discovery.

Canonical detail: [`internal/migration/programme-map.md`](../internal/migration/programme-map.md) and [`studies/again/zero-label/README.md`](../studies/again/zero-label/README.md).
