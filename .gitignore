# Heavy and machine-local artifacts live behind one external boundary.
/artifacts

# Migration and local working state.
/.migration-work
/.scratch
/.cache
/graphify-out

# Phase A evidence dashboard — local tool, not reviewed/committed yet.
/dashboard

# Environments, dependencies, and generated builds.
/.venv
**/__pycache__
**/node_modules
**/dist
**/build

# Host noise.
.DS_Store

# Wrangler CLI local state (holds the Cloudflare account id), not source.
/.wrangler
